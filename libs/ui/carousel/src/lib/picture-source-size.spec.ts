import { PictureSourceSize } from './picture-source-size';

describe('PictureSourceSize', () => {
  it('should create an instance', () => {
    const directive = new PictureSourceSize();
    expect(directive).toBeTruthy();
  });
});
